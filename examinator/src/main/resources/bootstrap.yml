spring:
  application:
    name: examinator
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
